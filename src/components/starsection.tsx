// styling changes:



// 1. the circles around the clickable stars should be implemented so when the user hovers over a star, it displays the circle rather than for every clickable point. in addition to this, add a random amount of small spheres on points around the circle and make the circle spin so it looks like planets orbiting this star (no more than 4 "planets" on each hover circle and "planets" should be randomly placed on the circle). also the star point name when hovering over a star clips into the circle and other stars/lines. i dont think it looks very professional but i cant think of a good solution.

// 2. when the user selects a star and it scrolls down, the top bar covers the section heading.

// 3. i want the top bar mapped to the the top of the page so when the user scrolls past it, it disappears rather than staying visible at the top of the page. this could be the solution to styling change #2. ill give you control over the styling changes #2 and #3

// 4. remove the thin 1 pixel line at the bottom of the top bar. make it invisible or remove.

// 5. on the homepage, reduce the padding between the section titles and section captions. also reduce padding between section captions and the top item of each section. then reduce padding between items in each section.
